name: 'Bootstrap'
description: 'Install curl on the runner'
runs:
  using: "composite"
  steps:
    - run: |
        sudo apt-get update
        sudo apt-get install -y curl
        sudo apt-get install -y build-essentials
      shell: bash
