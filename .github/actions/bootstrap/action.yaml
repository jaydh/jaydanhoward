name: 'Bootstrap'
description: 'Install curl on the runner'
runs:
  using: "composite"
  steps:
    - run: |
        sudo apt-get update
        sudo apt-get install -y curl build-essential pkg-config libssl-dev docker
        sudo systemctl start docker
      shell: bash
